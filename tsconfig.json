{
  // Solution-style config: owns no files itself, so the editor resolves every
  // file through the nearest project (server/ or client/) with its own
  // node_modules/@types (jest, vitest, node, ...).
  "files": [],
  "references": [{ "path": "./server" }, { "path": "./client" }, { "path": "./chrome-extension" }]
}
