/**
 * Commit message linting (husky commit-msg hook): conventional commits.
 * Types: build, chore, ci, docs, feat, fix, perf, refactor, revert, style,
 * test — subject in English.
 */
export default {
  extends: ['@commitlint/config-conventional'],
};
